# دليل إعداد نظام المصادقة - VirClinc Patient App

## 📋 نظرة عامة

تم إنشاء نظام مصادقة كامل لتطبيق Flutter يتضمن:
- شاشة تسجيل الدخول (Login Screen)
- شاشة التسجيل (Register Screen)
- خدمة API للاتصال بالباك اند
- خدمة المصادقة مع حفظ البيانات محلياً
- التحقق من صحة البيانات (Validation)

## 🏗️ البنية

```
lib/
├── config/              # نظام التصميم
│   ├── colors.dart
│   ├── text_styles.dart
│   ├── dimensions.dart
│   └── theme.dart
├── models/              # النماذج
│   ├── user.dart
│   ├── auth_response.dart
│   ├── login_request.dart
│   └── register_request.dart
├── services/           # الخدمات
│   ├── api_service.dart
│   └── auth_service.dart
├── screens/            # الشاشات
│   ├── auth/
│   │   ├── login_screen.dart
│   │   └── register_screen.dart
│   └── home/
│       └── home_screen.dart
├── widgets/            # المكونات
│   └── common/
│       ├── primary_button.dart
│       ├── app_input_field.dart
│       └── app_card.dart
└── utils/             # الأدوات المساعدة
    └── validators.dart
```

## 📦 المكتبات المطلوبة

تم إضافة المكتبات التالية في `pubspec.yaml`:
- `http: ^1.1.0` - للطلبات HTTP
- `shared_preferences: ^2.2.2` - لحفظ البيانات محلياً

## ⚙️ الإعداد

### 1. تحديث رابط API

افتح `lib/services/api_service.dart` وقم بتحديث `baseUrl`:

```dart
static const String baseUrl = 'http://localhost:3000/v1';
```

**ملاحظة مهمة:**
- للتطوير على Android Emulator: استخدم `http://10.0.2.2:3000/v1`
- للتطوير على iOS Simulator: استخدم `http://localhost:3000/v1`
- للأجهزة الفعلية: استخدم IP جهازك مثل `http://192.168.1.100:3000/v1`

### 2. تثبيت المكتبات

```bash
cd patien_app
flutter pub get
```

## 🚀 الاستخدام

### تشغيل التطبيق

```bash
flutter run
```

### API Endpoints المستخدمة

1. **تسجيل الدخول**
   - `POST /auth/login`
   - Body: `{ "email": "user@example.com", "password": "password123" }`
   - Response: `{ "access_token": "...", "user": {...} }`

2. **التسجيل**
   - `POST /auth/register/patient`
   - Body: `{ "name": "...", "email": "...", "phone": "...", "password": "..." }`
   - Response: `{ "id": "...", "email": "...", "name": "...", "role": "PATIENT" }`

## 📱 الشاشات

### شاشة تسجيل الدخول

- حقول إدخال: البريد الإلكتروني، كلمة المرور
- التحقق من صحة البيانات
- زر تسجيل الدخول
- رابط للانتقال إلى التسجيل
- عرض بيانات الاختبار

### شاشة التسجيل

- حقول إدخال: الاسم، البريد الإلكتروني، رقم الهاتف، كلمة المرور، تأكيد كلمة المرور
- التحقق من صحة جميع البيانات
- زر إنشاء الحساب
- رابط للانتقال إلى تسجيل الدخول
- بعد التسجيل الناجح، يتم توجيه المستخدم لتسجيل الدخول

## 🔐 آلية العمل

1. **تسجيل الدخول:**
   - المستخدم يدخل البريد وكلمة المرور
   - يتم التحقق من البيانات محلياً
   - يتم إرسال الطلب للباك اند
   - عند النجاح، يتم حفظ `access_token` وبيانات المستخدم محلياً
   - يتم الانتقال إلى الشاشة الرئيسية

2. **التسجيل:**
   - المستخدم يملأ جميع البيانات المطلوبة
   - يتم التحقق من صحة البيانات
   - يتم إرسال الطلب للباك اند
   - عند النجاح، يتم توجيه المستخدم لتسجيل الدخول

3. **التحقق من حالة تسجيل الدخول:**
   - التطبيق يتحقق تلقائياً من وجود token محفوظ عند البدء
   - إذا كان موجود، يتم توجيه المستخدم للشاشة الرئيسية
   - إذا لم يكن موجود، يتم توجيهه لشاشة تسجيل الدخول

## 🎨 التصميم

يتبع التطبيق مواصفات التصميم المحددة في `FLUTTER_DESIGN_SPECIFICATION.md`:
- نظام ألوان موحد (AppColors)
- أنماط خطوط موحدة (AppTextStyles)
- مسافات وحدود موحدة (AppDimensions)
- مكونات قابلة لإعادة الاستخدام

## 🧪 بيانات الاختبار

يمكنك استخدام البيانات التالية للاختبار:

**تسجيل الدخول:**
- البريد: `admin@clinic.com`
- كلمة المرور: `password123`

أو أي حساب موجود في قاعدة البيانات.

## 📝 ملاحظات مهمة

1. **RTL Support:** التطبيق يدعم العربية مع `Directionality`
2. **Error Handling:** جميع الأخطاء يتم معالجتها وعرضها للمستخدم
3. **Validation:** يتم التحقق من صحة البيانات قبل الإرسال
4. **Loading States:** يتم عرض حالة التحميل أثناء الطلبات
5. **Local Storage:** يتم حفظ بيانات المصادقة محلياً باستخدام `shared_preferences`

## 🔧 استكشاف الأخطاء

### مشكلة في الاتصال بالباك اند

1. تأكد من أن الباك اند يعمل على المنفذ المحدد
2. تحقق من `baseUrl` في `api_service.dart`
3. للتطوير على Android Emulator، استخدم `10.0.2.2` بدلاً من `localhost`

### مشكلة في التحقق من البيانات

- تأكد من أن جميع الحقول مملوءة بشكل صحيح
- البريد الإلكتروني يجب أن يكون بصيغة صحيحة
- كلمة المرور يجب أن تكون 6 أحرف على الأقل
- رقم الهاتف يجب أن يكون بصيغة `+1234567890`

### مشكلة في حفظ البيانات

- تأكد من أن `shared_preferences` مثبتة بشكل صحيح
- جرب حذف البيانات المحفوظة وإعادة تسجيل الدخول

## 📚 الخطوات التالية

1. إضافة شاشة Home كاملة
2. إضافة Bottom Navigation Bar
3. إضافة شاشات أخرى (Appointments, Departments, Profile)
4. إضافة إدارة الحالة (State Management) باستخدام Provider أو Bloc
5. إضافة دعم Offline Mode

---

**تم الإنشاء:** 2024  
**الإصدار:** 1.0.0

